# Dependencies
node_modules/

# Build output
dist/
src-tauri/target/
src-tauri/gen/
server/target/

# Server runtime data (SQLite db + dropped-in release binary)
server/*.db
server/*.db-wal
server/*.db-shm
server/web/Anode.exe

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local secrets (Claude API key lives in localStorage, but just in case)
.env
.env.local
